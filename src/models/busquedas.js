export class Busqueda {
	historial = [];

	constructor() {
		// TODO : Leer DB si existe
        console.log(this.historial)
	}

	async cuidad(lugar = '') {
		console.log(this.lugar);
		return []; // TODO: regresar arreglo de cuidades;
	}
}
